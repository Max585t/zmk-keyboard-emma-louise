# Delete all unused properties.

# Give your module a name
name: zmk-keyboard-emma-louise
build:
  # Kconfig filepath
  kconfig: Kconfig
  settings:
    # Folder containing a "boards" folder (. = root)
    board_root: .
    # Folder containing a "dts" folder (. = root)
    dts_root: .
    # Folder containing a "snippets" folder (. = root)
    snippet_root:  .
